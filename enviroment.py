class Enviroment:
    def make(env_name):
        if env_name == 'contextual':
            return
        elif env_name == 'simple':
            return
        elif env_name == 'random-graph':
            return
